<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.virtualbox.torchick.sitani">

    <uses-sdk tools:overrideLibrary="com.facebook.shimmer" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.virtualbox.torchick.sitani.Activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name="com.virtualbox.torchick.sitani.Activity.LinkDataFormActivity" />
        <activity android:name="com.virtualbox.torchick.sitani.Activity.WebActivity" />
        <activity android:name="com.virtualbox.torchick.sitani.Activity.PublikasiActivity" />
        <activity
            android:name="com.virtualbox.torchick.sitani.Activity.SplashscreenActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity android:name="com.virtualbox.torchick.sitani.Activity.LinkDataFormSearchActivity" />
        <activity android:name="com.virtualbox.torchick.sitani.Activity.LinkDataFormOfflineActivity" />
        <activity android:name="com.virtualbox.torchick.sitani.Activity.WebOfflineActivity" />
        <activity
            android:name="com.virtualbox.torchick.sitani.Activity.FabActivity"
            android:label="@string/title_activity_fab"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>